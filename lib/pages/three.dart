import 'package:flutter/material.dart';

class Three extends StatelessWidget {
  const Three({Key? key}) : super(key: key);
  static const routeName = '/three';

  @override
  Widget build(BuildContext context) {
    return Text('Three');
  }
}
